alias- gives alternative name to methods.
and- conditional statement that is inclusive of all conditions.
BEGIN- method that sets up blocks before anything else is executed.
begin- runs code that may raise exception
break- quits a block of code and from the method that contains the block
case - conditional statement that compares conditions and runs line of code if condition matches
class- model from which objects are created
def- defines a method 
defined?- checks if a variable is defined
do - defines arguments for a block of code, similar to for loops
else - conditional statement used if no cases match 
elsif - condition that is evaluated if previous condition is not met
END- runs code after everything 
end- ends a method or conditional statement that is defined
ensure- ensures code is always evaluated.
false- condition opposite to true
for - defines argument for a block of code and keeps variable living after the block is run.
if- conditional statement that evaluates a condition
in - condition that evaluates if something is in an array
module -creates or opens a module
next- breaks out of the block of code and runs the next block.
nil- null data value
not- conditional statement that evaluates if a statement is false
or- one condition or the other
redo- re-executes a block of code
rescue- starts an exception section of code in a begin block
retry- retries an exception block
return -exits a method
self- the object the current method is attached to
super- calls the current method in a superclass
then- Indicates the end of conditional blocks in control structures.
true- boolean  true
undef- Prevents a class or module from responding to a method call.
unless- The unhandled condition in case, if and unless expressions.
until- Creates a loop that executes until the condition is true.
when- a condition in a case expression
while-Creates a loop that executes while the condition is true.
yield -Starts execution of the block sent to the current method.

true  true and true
false false and true
nil  - empty data
constants - VAR1=100
strings - "This is a string."
numbers - 7875
ranges  - (1..5)
arrays - [1, 2 ,3]
hashes -Hash.new

 \\- front slash insert
 \’ - single quote insert
 \"- double quote insert
 \a - alert 
 \b - not including last character
 \f - not including beginning character
 \n - new line
 \r -inserts carriage return
 \t - tab
 \v - vertical tab
 
  ::- unary operator that allows: constants, instance methods and class methods defined within a class or module, to be accessed from anywhere outside the class or module.
  []- array
  **-exponent
  -(unary)- subtraction involving a single component or element
  +(unary)- ^ addition*
  !-not
  ~- complement
  *- multiply
  / - divide
  % - formatter
  +-addition
  <<-binary left shift
  >>-- binary right shift
  &- and
  |- binary OR
  >- greater then
  >= or equal to
  < less than
  <= or equal to
  <=>Combined comparison operator. Returns 0 if first operand equals second, 1 if first operand is greater than the second and -1 if first operand is less than the second.
  == Checks if the value of two operands are equal or not, if yes then condition becomes true.
  ===Used to test equality within a when clause of a case statement.
  != not equal
  =~ nearly equal (ignores floats)
  !~ not nearly equal (ignores floats
  && logical AND
  || logical OR
  .. INCLUSIVE RANGE
  ... EXCLUSIVE RANGE